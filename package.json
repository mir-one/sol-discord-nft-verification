{
  "name": "nft-verifier",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "app": "cd frontend && npm start",
    "discord":"cd discord && npm start --ignore app",
    "dev": "concurrently --kill-others \"npm run app\" \"npm run discord\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && (cd frontend && npm install --legacy-peer-deps) && (cd ../discord && npm install)"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}

